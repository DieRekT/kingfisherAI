# === OpenAI / Models ===
OPENAI_API_KEY=your-openai-key-here
AI_MODEL_CHAT=gpt-5-mini           # default; falls back automatically if unavailable
AI_MODEL_REASONING=o3              # cost-capped escalation
AI_MAX_OUTPUT_TOKENS=1200
AI_REQUEST_TIMEOUT_S=40
AI_USE_JSON_SCHEMA=1               # enable OpenAI JSON Schema structured outputs

# === Images (real-first) ===
UNSPLASH_ACCESS_KEY=your_unsplash_key
PEXELS_API_KEY=your_pexels_key
IMAGE_PROVIDER_ORDER=unsplash,pexels,generate

# === Search & Tools ===
SEARCH_PROVIDER=responses_api      # or "duckduckgo" for fallback
CACHE_TTL_S=1800                   # 30 min cache for search/images

# === App ===
APP_TIMEZONE=Australia/Sydney
APP_ORIGIN=http://localhost:3000   # CORS locked to this origin
APP_NAME=Kingfisher 2465

# === CI/Testing ===
CI=0                               # set to 1 for stub responses in tests
